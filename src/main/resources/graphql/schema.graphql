type User {
    id: ID!
    username: String!
    email: String!
}

type Query {
    getUser(username: String!): User
}

type Mutation {
    signUp(username: String!, email: String!, password: String!): User
}
